version: "3"

services:

  postgres-db:
    image: postgres:11-alpine
#    image: mysql:8.0.11
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: db
