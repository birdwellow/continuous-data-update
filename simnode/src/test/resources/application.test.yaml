spring:
  h2:
    console:
      enabled: true

 jpa:
   show-sql: true
   properties:
   hibernate:
     generate_statistics: true
     format_sql: true

logging.level.org.hibernate:
  stat: debug
  type: trace